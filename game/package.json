{
  "name": "dlikarts-vertical-slice",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python -m http.server 4173 --directory ..",
    "test": "node --test tests/*.test.mjs"
  }
}
